//learning import and export but for it we require a package.json file
//name and version are required in package.json 
//all else are optional
export default {
    f1:()=>{console.log('fxn is exported as an object')},
    //other fxn can be written as an object
};